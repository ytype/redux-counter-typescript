export type state = {
  toggle: boolean
  counter: number
}

export type action = {
    type: string
    difference?: number
}

export type initialState = {
    toggle: boolean,
    counter: number
}

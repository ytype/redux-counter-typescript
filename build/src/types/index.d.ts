export declare type state = {
    toggle: boolean;
    counter: number;
};
export declare type action = {
    type: string;
    difference?: number;
};
export declare type initialState = {
    toggle: boolean;
    counter: number;
};
